// This file is deprecated - using Firebase Auth instead
// Kept for reference only
// Exporting a valid Router to satisfy Convex deployment requirements

import { httpRouter } from "convex/server";

const http = httpRouter();

export default http;
